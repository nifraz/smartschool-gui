import { TitleCaseWithSpacePipe } from './title-case-with-space.pipe';

describe('TitleCaseWithSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new TitleCaseWithSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
